package:
  name: psdaq
  version: '{{ environ.get("GIT_DESCRIBE_TAG", "0.0.0") }}'

source:
  git_url: https://github.com/slac-lcls/lcls2.git

build:
  number: {{ environ.get('GIT_DESCRIBE_NUMBER', 0) }}

requirements:
  build:
    - cmake
    - make
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ cdt('libibverbs') }}
    - {{ cdt('libibverbs-devel') }}
    - {{ cdt('libnl') }}
    - {{ cdt('librdmacm') }}
    - {{ cdt('librdmacm-devel') }}
    - {{ cdt('libudev') }}
  host:
    - xtcdata
    - psalg
    - python
    - libfabric
    - epics-base >=7
    - prometheus-cpp
    - rapidjson
    - readline
    - zeromq
    - p4p
    - zlib
    - libusdusb4
  run:
    - xtcdata
    - psalg
    - python
    - libfabric
    - rapidjson
    - readline
    - transitions
    - requests
    - zeromq
    - prometheus_client
    - amityping
    - mypy
    - rogue=v5.2.3 # sometimes it will revert to old version (because of arrow?)
    - bluesky
    - ophyd
    - p4p
    - lcls2_timetool
    - wave8
    - cameralink-gateway
    - epics-base >=7
    - zlib
    - libusdusb4
